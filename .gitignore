# --- secrets: never commit these ------------------------------------------
.env
*.env
!.env.example
*.env.bak
client_secret.json
*credentials*.json
*token*.json

# --- your personal configuration ------------------------------------------
# config.json holds real names, calendars and your vault path. The tracked
# file is config.example.json — copy it to config.json and edit that.
backend/config.json

# --- your data -------------------------------------------------------------
backend/data/          # the item store: emails, events, prices
backend/config/        # portfolio.json — your holdings and share counts

# --- retired v1 code (kept locally for reference, not published) -----------
_v1_removed/

# --- build output and dependencies ----------------------------------------
node_modules/
frontend/dist/
dist/
build/

# --- noise -----------------------------------------------------------------
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
*.tmp
